public class set_info extends manager_tasks{
    public set_info(String name, String date, int priority, boolean completion_status) {
        super(name, date, priority, completion_status);
    }
}
